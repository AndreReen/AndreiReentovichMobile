/*
package scenarios;

import org.testng.Assert;
import org.testng.annotations.Test;
import setup.BaseTest;
import setup.TestDataProvider;


public class bonusEx2WiP extends BaseTest {

    @Test(description = "Performs registration and signs in to the app, checks BudgetActivity title text",
            dataProviderClass = TestDataProvider.class, dataProvider = "nativeTestData")
    public void nativeExerciseTest(String email, String name, String password, String title) throws Exception {

//        BufferedImage bImage = ImageIO.read(new File("C:\\Users\\ANDY-Z\\IdeaProjects\\AndreiReentovichMobile\\src\\main\\resources\\errorImg.png"));
//        ByteArrayOutputStream bos = new ByteArrayOutputStream();
//        ImageIO.write(bImage, "png", bos );
//        byte [] partialImage = bos.toByteArray();
//
//
//        byte[] screenshot = Base64.encodeBase64(getDriver().getScreenshotAs(OutputType.BYTES));
//
//
//        getNativePO().getHomePage().performLogin(email, password);
//
//        OccurrenceMatchingResult result = getDriver()
//                .findImageOccurrence(screenshot, partialImage, new OccurrenceMatchingOptions()
//                        .withEnabledVisualization());
//        assertThat(result.getVisualization().length, is(greaterThan(0)));
//        assertNotNull(result.getRect());

    }
}
*/
